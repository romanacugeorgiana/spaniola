# spaniola Your workflow going forward:

  1. Edit exercises in exercitii folder
  2. Prompt claude to add the new exercise to index.html (when adding new exercises, not modifying existing ones)
  2.1 check locally that everything works fine
  3. git add -A && git commit -m "wip" && git push